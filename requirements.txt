# LLM Frameworks
langchain>=0.3.0
langchain-openai>=0.2.0
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=2.0.0

# OpenAI-compatible embedding client (RAG / Memory vector; self-written lightweight retrieval, none LlamaIndex)
openai>=1.0.0
# Numerical backend for vector cosine retrieval (indexer / memory / graph / copilot direct import)
numpy>=1.26.0

# Optional local embedding dependencies live in requirements.local-embedding.txt

# PDF reading (for resume RAG)
pypdf>=4.0.0

# Web
fastapi>=0.115.0
uvicorn>=0.32.0
python-multipart>=0.0.12

# Storage
aiosqlite>=0.20.0

# Speech-to-text (Alibaba Cloud OSS + DashScope cloud ASR)
# OSS is only used for asynchronous transcription (recording duplication) of long audio; short audio uses a base64 synchronous link, without OSS.
oss2>=2.18.0

# Auth
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0

# HTTP (transcribe direct import; previously only relied on oss2/tavily transitive dependency)
requests>=2.31.0

# Web search (Copilot Company Researcher)
tavily-python>=0.3.0

# Copilot real time ASR (DashScope qwen3-asr-flash-realtime)
websockets>=12.0

# Tencent Cloud VPR (Optional, used for candidate voiceprint recognition)
tencentcloud-sdk-python-common>=3.0.1000

# VAD segmentation (C library, non-neural network)
webrtcvad>=2.0.10

# Utils
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
rich>=13.0.0
